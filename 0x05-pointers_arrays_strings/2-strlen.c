#include"main.h"
#include<stdio.h>

/**
 * _strlen - entry point
 *
 * @s: takes input
 *
 * Return: Always 0
 */

int _strlen(char *s)
{
	char *str;

	int len;



	str = "My first strlen!";

	len = _strlen(str);
	return(strlen);
}
