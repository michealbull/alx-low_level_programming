#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: printing lowercase letters with putchar
 *
 * Result: Always 0 (Success)
 */

int main(void)
{
	putchar(a - z);
	putchar(\n);

	return (0);
}
